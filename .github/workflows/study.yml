name: Github_Actions_Study

on:
  push:
    branches: ["main"]

jobs:
  directory-print:
    runs-on: ubnutu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: print
        run: ls -a
