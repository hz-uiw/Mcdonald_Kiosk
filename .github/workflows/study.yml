name: Github_Actions_Study

on:
  push:
    branches: ["main"]

jobs:
  directory-print:
    runs-on: ubnuntu-latest
    steps:
      - uses: actions/chekout@v4
      
      - name: print
        run: ls -a
